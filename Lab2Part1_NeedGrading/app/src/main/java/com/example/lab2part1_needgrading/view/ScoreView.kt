package com.example.lab2part1_needgrading.view

import androidx.compose.runtime.Composable
import com.example.lab2part1_needgrading.controller.ScoreController

@Composable
fun ScoreView(scoreController: ScoreController) {
    // Composable that takes the current score from the ScoreController and displays it
}
